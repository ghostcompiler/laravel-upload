{
    "name": "ghostcompiler/uploads-manager",
    "description": "Manage your uploads easily",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "^8.1",
        "illuminate/support": "^9.0 || ^10.0 || ^11.0 || ^12.0 || ^13.0"
    },
    "suggest": {
        "ext-gd": "Used for image compression plus AVIF/WEBP conversion when supported by the PHP build.",
        "ext-imagick": "Used as an automatic fallback for AVIF/WEBP conversion when GD support is unavailable or limited.",
        "laravel/framework": "Required in the host application to provide Eloquent, routing, console, HTTP, and filesystem integration."
    },
    "autoload": {
        "psr-4": {
            "GhostCompiler\\UploadsManager\\": "src/"
        },
        "files": [
            "src/helpers.php"
        ]
    },
    "extra": {
        "laravel": {
            "providers": [
                "GhostCompiler\\UploadsManager\\UploadsManagerServiceProvider"
            ],
            "aliases": {
                "Uploads": "GhostCompiler\\UploadsManager\\Facades\\Uploads"
            }
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "autoload-dev": {
        "psr-4": {
            "GhostCompiler\\UploadsManager\\Tests\\": "tests/"
        }
    },
    "require-dev": {
        "orchestra/testbench": "^7.0 || ^8.0 || ^9.0 || ^10.0",
        "phpunit/phpunit": "^10.0 || ^11.0 || ^12.0"
    },
    "authors": [
        {
            "name": "Ghost Compiler"
        }
    ]
}
